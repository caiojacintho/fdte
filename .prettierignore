node_modules/
dist/
build/
*.tsbuildinfo
server/data/
**/src/assets/
.omo/
.codegraph
.claude/
package-lock.json

# CSS formatting is out of scope for this baseline (only .ts/.tsx style was
# sampled); avoid unrelated diff noise in stylesheets.
**/*.css

# Auto-generated data (417 IBGE municipality names), explicitly commented
# "não editar à mão" in the file itself — its double-quote style comes from
# whatever script produced it, not from hand-authoring, so leave it as-is.
formulario/src/data/citiesBahia.ts

# Auto-generated by `drizzle-kit generate` (migration SQL + metadata
# snapshots/journal) — committed as the tool produces them so future
# `drizzle-kit generate` runs don't fight Prettier's formatting.
server/drizzle/
